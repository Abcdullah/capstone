<%= form_with(model: record_datum, local: true) do |form| %>
  <% if record_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(record_datum.errors.count, "error") %> prohibited this record_datum from being saved:</h2>

      <ul>
      <% record_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :user %>
  </div>

  <div class="field">
    <%= form.label :post %>
    <%= form.text_field :post %>
  </div>

  <div class="field">
    <%= form.label :upvotes %>
    <%= form.number_field :upvotes %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="field">
    <%= form.label :totalViews %>
    <%= form.number_field :totalViews %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
